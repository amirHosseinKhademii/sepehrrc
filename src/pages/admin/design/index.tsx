import { DesignContainer } from 'containers';
import { DesignLayout } from 'components';

const index = () => {
  return (
    <DesignLayout>
      <DesignContainer />
    </DesignLayout>
  );
};

export default index;
