import { Design } from 'containers';
import { DesignLayout } from 'components/admin/layouts';

const index = () => {
  return (
    <DesignLayout>
      <Design />
    </DesignLayout>
  );
};

export default index;
