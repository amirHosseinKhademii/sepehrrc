import { StoreFrontContainer } from 'containers';
import { DesignLayout } from 'components';

const index = () => {
  return (
    <DesignLayout>
      <StoreFrontContainer />
    </DesignLayout>
  );
};

export default index;
